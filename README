mkdir -p ~/.local/bin && gcc -w -std=gnu89 -o ~/.local/bin/e e.c && grep -q 'export PATH="\$HOME/.local/bin:\$PATH"' ~/.bashrc || echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/.local/bin:$PATH"
