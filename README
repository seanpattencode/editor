# Startup time (ms, hyperfine)

| e    | ls   | nano | micro | nvim | vi   |
|------|------|------|-------|------|------|
| 0.56 | 1.8  | 0.79 | 2.5   | 16.9 | 22.3 |

e opens a 5,600-line file in 1.1ms. nvim takes 175ms (159x slower).
e renders a full interactive directory browser 3x faster than ls prints filenames.

bench/bench.sh

# Build & install (self-compiling, requires gcc/clang)

sh e.c              # build
sh e.c install      # build + symlink to ~/.local/bin/e
sh e.c debug        # ASan/UBSan debug build
sh e.c clean        # remove binary

# Use pre-built binary (no compiler needed)

mkdir -p ~/.local/bin && cp ~/projects/editor/e ~/.local/bin/ && chmod +x ~/.local/bin/e && grep -q 'export PATH="$HOME/.local/bin:$PATH"' ~/.bashrc || echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/.local/bin:$PATH"
